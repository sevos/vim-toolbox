%section.actions
  = link_to "Submit plugin", new_plugin_path
#plugins
  - @plugins.each do |plugin|
    .plugin
      .counter
        = plugin.add_to_toolbox_button
      %h1= plugin.repository
      %p= plugin.description
