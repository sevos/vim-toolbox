section.content {
  background-color: $color-bg-content;
  color: $color-fg-content;

  section.actions {
    text-align: right;
    @include row();
    margin-bottom: 1.5em;

    a {
      @include button(pill);
    }
  }

  h1 {
    text-align: center;
  }

  #plugins {
    @include row();

    .plugin {
      @include pad();

      background-color: #fff;
      margin-bottom: 1em;
      border-radius: 10px;

      h1 {
        margin: 0;
        display: block;
        @include disable-link-decoration;
      }

      p {
        color: #444;
      }
    }
  }

  #new-plugin {
    form {
      @include span-columns(6 of 8);
      @include shift(1);
      text-align: center;

      section {
        @include row;
      }

      label {
        float: left;
        line-height: 32pt;
      }

      input[type=text] {
        float: right;
        background-color: $color-bg-content + #222;
        border: 2px solid $color-fg-content;
        color: $color-fg-content;
        font-size: 24pt;
        text-align: center;
      }

      a.back {
        disable-link-decoration($color-fg-content)
      }

      input[type=submit] {
        @include button;
        margin-top: 1em;
        font-size: 120%;
      }
    }
  }
}
